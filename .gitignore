# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# YOLO inference output dumps (regenerated by every predict() call, not
# source). Training run configs under runs/detect/train*/args.yaml are kept.
runs/detect/predict*/
yolo_output_final_warped/

# Ad hoc debug images written by commented-out plt.savefig()/Image.save()
# calls in pipeline.py; not referenced by any active code path.
/Figure_1.png
/p_test.jpg
/warped_image.jpg
/warped_image_with_detects.jpg
/chessboard_transformed_with_grid.jpg

# Abandoned Harris/contour corner-detection experiments, not part of the
# working pipeline (see CLAUDE.md "Known rough edges"). Kept on disk, not
# tracked going forward.
/non_production_archive/
